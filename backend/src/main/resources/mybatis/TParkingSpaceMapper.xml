<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plms.mapper.TParkingSpaceMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.plms.model.entity.TParkingSpace">
        <id column="space_id" property="spaceId" />
        <result column="floor_id" property="floorId" />
        <result column="space_no" property="spaceNo" />
        <result column="status" property="status" />
        <result column="is_fixed" property="isFixed" />
        <result column="owner_id" property="ownerId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        space_id, floor_id, space_no, status, is_fixed, owner_id
    </sql>

</mapper>
